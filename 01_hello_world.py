		# Bienvenido a las primeras lecciones de programación. Utilizaremos el lenguaje Python por su versatilidad
# y abstracción al alto nivel (no se ven cosas complejas como manejo de memoria)	
#
# El primer programa que se realiza SIEMPRE es hacer que se imprima en la consola el mensaje "hola mundo".
# Para esto debemos tener en cuenta dos cosas fundamentales:
# 1. Las cadenas de caracteres (el texto simple y llano) deben estar rodeadas 
#    por comillas simples (') o comillas dobles (")
#    De esta manera, para que el sistema interprete que queremos imprimir una frase 
#    (como ser: puto el que lee (caiste, Ale)), debemos definirla como "puto el que lee" o 'puto el que lee'. 
#    Es importante que el texto se abra y cierre con el mismo tipo de comilla (simple o doble)
# 2. Cada lenguaje tiene su forma particular de llamar a la impresión por consola. 
#    En el caso de Python se usa la función:
#    print(texto)
#
# Un último detalle (por ahora): estas líneas que comienzan con numeral (hash, #) no son tenidas en cuenta
# por el intérprete de código. Para comenzar a desarrollar, hay que escribir sin el numeral al principio.
#
# Ahora sí: escribí tu primer Hola Mundo!		
